<ul>
    {{#each cats}}
    <li>
        <img src="images/{{this.imageLocation}}.jpg" width="250" height="250" alt="Card image cap">
        <div class="info">
            <button class="showBtn">Show status code</button>
            <div class="status" style="display: none" id={{this.id}}>
                <h4>Status Code: {{this.statusCode}}</h4>
                <p>{{this.statusMessage}}</p>
            </div>
        </div>
    </li>
    {{/each}}
</ul>

{{!-- The Request --}}

{{!-- 

await fetch('./cats-template-ul.hbs')
    .then(res => res.text())
    .then(ulTemplate => {
        let template = Handlebars.compile(ulTemplate);
        articleCats.innerHTML = template({cats});
    })
    .catch(err => alert(`Error: ${err.message}. Try Again!`));

 --}}